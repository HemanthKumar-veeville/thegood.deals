name: Frontend CI/CD Pipeline

on:
  push:
    branches:
      - main # Production branch
      - development # Staging branch
  pull_request:
    branches:
      - main
      - development

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "16" # Specify your Node.js version

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build # Assumes a build script is defined in package.json

      - name: Archive production build artifacts
        if: github.ref == 'refs/heads/main' # Only archive for production
        run: tar -czf production-build.tar.gz ./dist

      - name: Archive staging build artifacts
        if: github.ref == 'refs/heads/development' # Only archive for staging
        run: tar -czf staging-build.tar.gz ./dist

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-artifact
          path: |
            production-build.tar.gz
            staging-build.tar.gz

  deploy-production:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' # Deploy only if on the main branch

    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v2
        with:
          name: build-artifact
          path: ./ # Download artifacts to the current directory

      - name: Deploy to Production Server
        env:
          HOST: ${{ secrets.PROD_SERVER_HOST }}
          USER: ${{ secrets.PROD_SERVER_USER }}
          KEY: ${{ secrets.PROD_SERVER_KEY }}
        run: |
          echo "$KEY" > prod_key.pem
          chmod 600 prod_key.pem
          scp -i prod_key.pem -o StrictHostKeyChecking=no production-build.tar.gz $USER@$HOST:/path/to/production
          ssh -i prod_key.pem -o StrictHostKeyChecking=no $USER@$HOST "
            cd /path/to/production &&
            tar -xzf production-build.tar.gz &&
            npm install
          "

  deploy-staging:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/development' # Deploy only if on the development branch

    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v2
        with:
          name: build-artifact
          path: ./ # Download artifacts to the current directory

      - name: Deploy to Staging Server
        env:
          HOST: ${{ secrets.STAGING_SERVER_HOST }}
          USER: ${{ secrets.STAGING_SERVER_USER }}
          KEY: ${{ secrets.STAGING_SERVER_KEY }}
        run: |
          echo "$KEY" > staging_key.pem
          chmod 600 staging_key.pem
          scp -i staging_key.pem -o StrictHostKeyChecking=no staging-build.tar.gz $USER@$HOST:/path/to/staging
          ssh -i staging_key.pem -o StrictHostKeyChecking=no $USER@$HOST "
            cd /path/to/staging &&
            tar -xzf staging-build.tar.gz &&
            npm install
          "
